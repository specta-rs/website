---
title: Flight Science
hidden: true
---

This is an outline of what i've been able to achieve with the Flight Science sponsorship.

# Phase 1 (completed)

From 2/12/2025 to 9/01/2026

## General

These things are pretty self-explainatory:
 - General repo maintenance including upgrading to edition 2024.
 - `diagnostic::on_unimplemented` for better errors - [#401](https://github.com/specta-rs/specta/issues/401)
 - Official `geojson` crate integration - [#437](https://github.com/specta-rs/specta/pull/437) - will merge after (#430) allowing you to remove your fork
 - Official `geozero` crate integration - [#436](https://github.com/specta-rs/specta/pull/436) - will merge after (#430) allowing you to remove your fork
 - Heavily reduce the usage of `specta::internal`.
 - Rename `export` to `collect` - [#388](https://github.com/specta-rs/specta/pull/388)
 - `std` feature like Serde has - [#437](https://github.com/specta-rs/specta/pull/437)
 - Specta attribute bug - [#393](https://github.com/specta-rs/specta/issues/393)
 - Migrate tests to `insta` for Snapshot testing - [#434](https://github.com/specta-rs/specta/pull/434)
 - Fixed flood of warnings and Clippy issues when compiling
 - Drop `#[specta(rename_from_path = ...)]` attribute. It was only ever used by PCR as far as I know and undocumented.

Bugs:
 - Typescript header being exported twice [#414](https://github.com/specta-rs/specta/issues/414)
 - Small TS exporter bug - [#231](https://github.com/specta-rs/specta/issues/231)
 - Prevent ctor segfaults if Specta stack overflows when using `export` feature - [#324](https://github.com/specta-rs/specta/issues/324)
 - Fix invalid output from when exporting w/ TS namespaces - [#409](https://github.com/specta-rs/specta/issues/409)

Minor:
 - Remove `specta::json` as it's useless in it's current form and i'm unsure whether it would be possible without major tradeoffs (tracked in [#94](https://github.com/specta-rs/specta/issues/94)) - [bbb9c01](https://github.com/specta-rs/specta/commit/bbb9c019264ff389a4f65feca6b3f4d79f796d62)

## Reference overhaul

I have done a massive refactor of how references work. References are core to Specta as they are how a single type can refer to another type in it's output (Eg. `{ property: TheReferencedType }` contains a property with a reference to`TheReferenceType`).

All of these related changes where merged in a larger PR [#423](https://github.com/specta-rs/specta/pull/423)

### Remove SpectaID

In Specta we need a unique identifier for each type to properly deduplicate them when exporting, so a type can only every show up once in a `TypeCollection`.

One issue in older versions of SpectaID is that types where assigned an identifier in the `specta::Type` macro, but it is also useful to be able to create dynamic types and export them at runtime. My original solution on `main` prior to this refactor was using an `AtomicUsize` stored on the `TypeCollection` and incrementing it to give each runtime type a unique ID.

This causes major DX issues with `TypeCollection` because if it can be `Clone`d then the `Atomic` would reset and potentially issue overlapping identifiers. One potential solution is that you could wrap it the `AtomicUsize` in an `Arc` to allow it to be shared but this introduces issues with which type collection is responsible for a type as they could branch when cloned but still issue duplicate identifiers from that point forward. This is all confusing and something we don't want end-users to have to think about.

I have rebuilt the system to internally use a custom `ArcId` type which hashes the pointer to a heap allocated or static reference. This gives a unique identifier without us needing to uphold the uniqueness guarantees as that's done by the Rust compiler. All references to a type hold the `ArcId` so the ID (pointer) can never be reused until every single reference is dropped, meaning we can't issue overlapping identifiers *ever*. This also allowed us to restore `Clone` on `TypeCollection` without introducing any confusion. Now supporting runtime types is something that just works how you would expect it.

### Drop `DataType::Literal`

In replacing SpectaID I went a step forward and decided to make these identifiers an implementation detail. The main advantage to this is that it removes a user-facing concept simplifying the library and also makes it much harder to abuse Specta's internals.

It does come with some downsides. Primarily that it makes comparision against a type a bit more complicated so to fix this I introduced `Reference::ref_eq` (inspired by [`Arc::ptr_eq`](https://doc.rust-lang.org/stable/std/sync/struct.Arc.html#method.ptr_eq)).

Now that SpectaID is hidden one issue I ran into is that `specta-serde` keeps track of references it has crawled, this way is to prevent it stack overflowing on recursive types, however this was previously expressed as `HashSet<SpectaID>` which is no longer possible. With these changes I decided to make `Reference` implement `Hash` which in tern required making `DataType` implement `Hash` as `Reference` holds a list of generics (Eg. `{ property: MyReference<String> }`, where the generic is the `DataType` returned from `<String as specta::Type>::reference()`). `DataType` never implemented `Hash` due to `DataType::Literal(LiteralType::f64(...))` which is not `Hash` due to `f64: !Hash`.

One unexpected advantage of this change is that because `HashSet<Reference>` hashes the entire `Reference` including the generics, `specta-serde` will process each generic being passed to a referenced type separately which is actually more correct than the previous implementation as for example if the generic is used in a flatten field, the value of the generic would affect whether the type should be rejected or not. This was an awesome thing to fall out of the refactor I didn't even know was a problem.

It's possible supporting const generics will require us to review this decision as a const `f64` might need to be put into the `DataType` enum but for now this is a massive improvement to what we had. I also wonder if because a const `f64` has to come from the compiler (not runtime) we can express it in a different form (maybe `&'static str`) which can be safely hashed as we don't need to support floating-point math on it. However I need to read more about Rust's rules of floating point numbers in const contexts to understand if this is safe or not.

### Opaque references

Sometimes you want to implement `Type` for a Rust-type which has an exporter-specific meaning. For example `Channel` in Tauri doesn't actually mean anything to the Swift exporter, it only means something to the Typescript exporter as it's aware of the Tauri JS runtime which has a Javascript class to represent a `Channel`.

With this I have build a system for opaque references (here is a real-world example for `specta_typescript::Any`):

```rs
pub struct Any<T = ()>(T);

pub(crate) static ANY_REFERENCE: Reference = Reference::opaque_from_sentinel({
    static SENTINEL: () = ();
    &SENTINEL
});

impl<T> Type for Any<T> {
    fn definition(_: &mut TypeCollection) -> DataType {
        // This just be `Reference::opaque()` but it *must* be cloned and identicial on each call.
        // You could have a `OnceLock<Reference>` which is simple but it would require the memory allocated by the `Reference::opaque()` call to exist for the lifetime of the program.
        // Using `Reference::opaque_from_sentinel` doesn't allocate a reference but is also a lot more error prone. Eg. using `const` instead of `static` will break it's invariants.

        DataType::Reference(ANY_REFERENCE.clone())
    }
}
```

Then in the Typescript exporter we do something like:
```rust
fn export_reference(reference: &Reference) -> String {
    // Note: This is a simplification of the real-world. We actually use "exporter references" to implement this (but they are explained in the next section).
    if reference == &ANY_REFERENCE {
        "any".to_string()
    } else {
        format!("{}", reference.name()) // This also handles generics, and more in the real-world implementation.
    }
}
```

In result this allows us to do something like the following:
```rust
#[derive(Type)]
pub struct Something {
    #[specta(type = specta_typescript::Any)]
    pub field: SomeNonSpectaType,
}
```

I am also thinking of moving Tauri Specta and TauRPC to use this for supporting `tauri::Channel` as the `TypeCollection` manipulation we used to do is no longer supported. This solution is much more robust as the exporters can detect unhandled opaque references and give useful errors instead of the manual manipulation causing internal panicking due to broken invariants of `TypeCollection`.

### Exporter references

In Tauri Specta and TauRPC's exporter code they express custom Typescript objects similar to the following:

```rust
format!("{{ some_property: {} }}", specta_typescript::primtives::export(...));
```

Which looks simple in this example but then they want to export Rust documentation comments, deprecated attributes which requires a JSDoc builder.

And then they want to handle cross-file imports for multi-file exporters, and more.

and very quickly your reimplementing half of `specta-typescript` in the downstream framework, but your custom implementation doesn't benefit from the rigor of Specta's tests and is a pain to maintain.

The core reason why these types are usually manually constructed in strings instead of being given to Specta is because they want to export things which can't be expressed by `DataType`. For example Tauri Specta exports something like:

```ts
export const commands = {
  my_command: (...) => TAURI_INVOKE("command", ...)
}
````

a closure and it's runtime code can't be expressed by `specta::DataType` because that would require being able to represent all Typescript syntax 😅.

So exporter references are the solution I came up with. They look like the following:

```rust
let ts = specta_typescript::Typescript::default();

let mut s = specta::datatype::Struct::named();
for command in commands {
    // Some logic to build up the closure here

    let mut field = specta::datatype::Field::new(self.define(format!("({arguments}) => {body}")).into());
    field.set_deprecated(command.deprecated().cloned());
    field.set_docs(command.docs().clone());
    s = s.field(command.name().to_lower_camel_case(), field);
}

println!("{}", specta_typescript::primitives::inline(self, cfg.types, &s.build())?);
```

The way this works is similar to opaque references where `Typescript` holds a `Vec<(Reference, Cow<'static, str>)>,` and then in `export_reference` we do something like:

```rust
fn export_reference(reference: &Reference) -> String {
    if let Some((_, typescript)) = ts.references.iter().find(|(re, _)| re.ref_eq(r)) {
        s.push_str(typescript);
        return Ok(());
    } else {
        format!("{}", reference.name()) // This also handles generics, and more in the real-world implementation.
    }
}
```

This results of this are:
 - Easier maintaince for TauRPC and Tauri Specta (once they adopt the new API)
 - More reliable due to a single shared implementation across Specta-based frameworks which can be well tested
 - Mostly-support for multi-file exporting without Tauri Specta and TauRPC needing to think about type imports
 - More consistent bindings format (Specta can have a consistent reasonable default for people who disable formatting on that file)
 - Implicit JSDoc support. Eg. `specta_typescript::Typescript` can be rewritten as `specta_typescript::JSDoc` and it should *just work*, no other changes (*unless the runtime injects custom code into the bindings file*)

## Drop `NamedType` and `Flatten`

Both of these were good ideas but i'm not sure they belong in core.

`NamedType` is a nice convience when doing type-map manipulation but I am trying to design newer API's to make that much harder because it breaks assumptions that Specta's core and exporters depend on. I also believe we can provide better ways to achieve the same sort of things (especially in related with the `Reference` overhaul). Closed by [#429](https://github.com/specta-rs/specta/pull/429)

`Flatten` was designed for compile-time validation of whether flattening is valid. Serde don't do this themselves and I think we have kinda figured out why. It's hard to properly tell in all scenarios and with the move to unbake Serde from Specta's core for better separation of concerns this feature is no longer worth it.

Both of these macros where also commonly forgotten when doing a custom `impl Type for T`, so removing them is nice to simplify the core and reduce bugs with Specta. They were commonly implemented incorrectly in Specta's own official crate implementations (😅) which isn't a great sign of a good API.

## Specta Serde is reborn

[#430](https://github.com/specta-rs/specta/pull/430) - incomplete but getting very close

It became clear very early in Specta's development that it can be a very powerful general introspection tool. This is what brought about support for more langauges than just Typescript and it has even be intended to extend to data formats. Although most of the ecosystem use [Serde](https://serde.rs), it's not the only possible option and we had started work on trying to break more of the Serde support out of the Specta core. This is where `specta-serde` started but i've always been hazzy on exactly how this idea was going to be achieved.

This This PR does a massive refactor which moves the handling of Serde attributes from the Specta macro to the runtime `specta-serde` crate opening up the ability for other formats to implement their own userspace support.

The main concept is that when you pass a type with attributes all of them are visible in the `TokenStream` Specta's macros see. For example:

```bash
#[derive(Type, MyMacro)]
pub struct Testing {
    #[my_attribute(something = "abc")]
    pub testing: String,
}
```

With the new Specta system when we create `DataType::Struct` we also include a lowered version of the raw `TokenStream` passed to `my_attribute` on the `Field`. The same is done for enum variant and contrainer attributes. Then the runtime code in `specta-serde`/any userspace implementation can parse the raw-attributes in whatever way it wants and it can tranform the `DataType` before it's given to `specta-typescript` to implement tagging, renames and more.

Supporting multiple formats is cool but how is this generally useful? This enables per-phase attributes.

For example `specta_serde` can expose:
```rust
// This is not an actual API but it shows the idea better and the final API is undecided.

pub fn transform_for_serialize(dt: DataType) -> DataType;

pub fn transform_for_deserialize(dt: DataType) -> DataType;

pub fn transform_for_both(dt: DataType) -> DataType;
```

When paired with Specta Typescript you can export your type and account for attributes which are differently by Serde between `Serialize` and `Deserialize`. In practice this feature is a bit advanced for an average user but my vision is that TauRPC and Tauri Specta will just handle this internally and end-users don't even need to know about it.

<Callout>This PR implements the Specta core support for per-phase attributes but doesn't actually expose the data from the Serde crate at the moment as it's unclear what the correct API is. <br /> <br /> One major concern I have is that if a type is used by the user for both serialization and deserialization it will need to be exported twice in the bindings file which I could see users disliking. I have some ideas to fix this but for now the Specta core support is the main focus, I can iterate on the high-level APIs later.</Callout>

This PR also aims to implement many missing Serde attributes.

#258 & #232 & #332 & #229 & #328 & #332 & #114 & #229 & #402 & #395 & #132 & #130 & #22

This PR should close the following issues:
 - [#258](https://github.com/specta-rs/specta/issues/258)
 - [#232](https://github.com/specta-rs/specta/issues/232)
 - [#332](https://github.com/specta-rs/specta/issues/332)
 - [#229](https://github.com/specta-rs/specta/issues/229)
 - [#328](https://github.com/specta-rs/specta/issues/328)
 - [#114](https://github.com/specta-rs/specta/issues/114)
 - [#402](https://github.com/specta-rs/specta/issues/402)
 - [#395](https://github.com/specta-rs/specta/issues/395)
 - [#132](https://github.com/specta-rs/specta/issues/132)
 - [#130](https://github.com/specta-rs/specta/issues/130)
 - [#22](https://github.com/specta-rs/specta/issues/22)

It also makes progress towards another 5-or-so more but they require more specific work.

One major thing to note with this change is that previously the following would compile:
```rust
#[derive(Specta)]
#[serde(untagged)] // Any `serde` attributes.
enum Testing { ... }
```

but with the new system `serde` attributes are treated as unknown by Specta so the user will be asked by Rust to include `serde::Serialize`. This is great as in a real-world senario if your using a Serde attribute, you will need to Serde runtime on your type.

Another example of this is that previously:
```rust
#[derive(Specta)]
#[specta(untagged)] // Any `serde`-format attributes passed to `specta`
enum Testing { ... }
```

would work as we parsed the arguments from `specta` and `serde` attributes into the same arguments struct. This will fail to compile as now only Specta-specific attributes can be pased to `#[specta(...)]`. This is great as in practice `#[specta(untagged)]` is not processed by `serde::{Serialize, Deserialize}` so this code would have been broken.

Sadly as this PR is a large refactor and new paradigm, I expect some regressions in the short-term but unit testing will help to iron these out. I am likely going to try and upgrade some real-world codebases to get a better idea of the impact on regular library users to see if a codemod or better guidance can be provided.

## JSDoc rework

This is required to upgrade Tauri Specta as it heavily depends on it. JSDoc is also used for the Typescript exporting of the deprecated attribute and comments. The support for exporting doc comments and deprecated attributes on struct fields and enum variants with Typescript is also a little buggy in `main` due to this.

The JSDoc API is still not complete but it's reusing much more of the Typescript exporters code and is now exposed as `specta_typescript::JSDoc` instead of `specta_jsdoc::JSDoc` as it is not worth it when JSDoc needs so many of the Typescript exporters internals (it's literally a `pub struct JSDoc(crate::Typescript);` where `Typescript` has a hidden `jsdoc: bool` field for slightly adjusting the output).

## Incomplete

Things i've started work on but been unable to finish due to technical constraints. I will come back to these once i've had some more time to work/think.

### TauRPC upgrade

I have started upgrading TauRPC to the `main`. This is a little bit of work with some of the breaking changes and with knowledge of some future ones I am going to make i've been trying to take them into account.

With the new "framework references" feature I think we can also simplify the exporter code a heap but this is going to be something for once the idea is proven out in Tauri Specta as I can afford to ship beta Specta APIs inside Tauri Specta as it's me who maintains it.

[#58](https://github.com/MatsDK/TauRPC/pull/58)

### Drop `#[specta::specta]` from core

The whole function handling system is too tied to Tauri Specta, and there has been discussion in the Tauri working group about ideas of a non-macro command system. On top of that it's always been an idea for me to try and hide `#[specta::specta]` under the `#[tauri::command]` macro as having both is bad DX.

I have done up a PR for removing this stuff but it would be a breaking change to the code that is within the Tauri crate so this is going to need to be approached with caution.

Also with the introduction of `Reference::opaque` I also want to change the `Type` implementation for `Channel` which is going to be rough.

This work is generally skewed towards Tauri Specta as TauRPC doesn't use the function macros from my knowledge (it only uses the `tauri::Channel` implementation) but these changes are *major* blockers for the next Specta release going forward.

I came up with a prototype for the minimal changes to Tauri to allow us to drop `specta::specta` from Tauri Specta [here](https://github.com/oscartbeaumont/tauri/commit/5719486dc97a17f90e94d018505eaa31677960c5). These need some more cleanup before I will do an upstream PR but it's promising. I also chatted with Lucas from the Tauri core-team and he seemed to be able to get behind it but i'm not sure if everyone will be thrilled by these changes as they are basically Tauri Specta-specific code in the Tauri code (although a very minimal amount).

This also closes function system related issues as not planned:
 - [#379](https://github.com/specta-rs/specta/issues/379)
 - [#213](https://github.com/specta-rs/specta/issues/213) - leaving open until we have a solution with the next version of the function integration.

[#432](https://github.com/specta-rs/specta/pull/432)

# Phase 2

From 9/01/2026 to 30/01/2026

## General

These things are pretty self-explainatory:
 - Support for [bytes](https://docs.rs/bytes) - [ref](https://github.com/specta-rs/specta/commit/40e77f9e38cf8a6c715f045f44a851a2c890bc3f)
 - Support for renaming the `selection!` struct - [ref](https://github.com/specta-rs/specta/commit/9e2fc59b06fbd89692dc2311c17a938b4b98804d)
 - Finish off and merge runtime attribute handling from phase 1 -[#430](https://github.com/specta-rs/specta/pull/430)
 - Add `TypeCollection::merge` - [ref](https://github.com/specta-rs/specta/commit/ccfd7d760ec3d7c0e5851c4399f1ff60656ce1e3)
 - Make `specta_typescript::primitives::reference` take `Reference` instead of `DataType` for correctness - [ref](https://github.com/specta-rs/specta/commit/862b52e756340b794f929e65f52554197b7908bf)

## References v2

The reference system overhaul that was implemented in Phase 1 was a remarkable improvement but in spending some time thinking of how we can support branded types I noticed that the system still could use some more work.

One major change is the realisation that references break down into 3 categories:
 - Named static (Eg. `pub struct` with derive `Type`)
 - Named dynamic (Eg. `NamedDataTypeBuilder::new(...).build(...)`)
 - Opaque - A reference to a string in the exporter (usually of raw-Typescript code) (Eg. `specta_typescript::Any`)

The major issues with the current system are:
 - You can't differentiate between a named dynamic and opaque type as they are both `ArcId::Dyanmic`.
 - You can `Reference::get` on an opaque type. This makes no sense because they are always inlined.
 - You can insert an opaque type into the `TypeCollection` due to the shared `ArcId`. This makes no sense because they are always inlined.

We had effectively conflated named dynamic and opaque types as one concept which isn't true in practice.

So to fix this we defined `Reference` to be the following:
 ```rs
pub enum Reference {
    Named(NamedReference),
    Opaque(OpaqueReference),
}

pub struct NamedReference {
    // `ArcId` becomes `NamedId`.
    pub(crate) id: NamedId,
    pub(crate) generics: ...,
    pub(crate) inline: bool,
}

// Technically `dyn Any` is `dyn DynOpaqueReference` but that's too in the weeds for this.
pub struct OpaqueReference(Arc<dyn Any>);
```

With this restructure the benefits we get are:
 - Can no longer insert or get opaque type into `TypeCollection` (Eg. `NamedReference::get` can't be called with an opaque reference)
 - Can no longer access `inline` or `generic` attributes of opaque type as they are on `NamedReference`.
 - Language exporters can detect unhandled opaque references and give a helpful error. (Eg. if you pass `specta_typescript::Any` to the Swift/Go/etc exporter)

Another change is that the previous opaque reference system was structured. Each new `Reference` was a "unique" pointer that had to be carefully cloned to ensure it pointed to the same type. This is what lead to the very unergonomic `Reference::opaque_from_sentinel` API.

With the new implementation:
 - References are nominal (compared by value)
 - References now have a value (`T`) which means they can hold data.

The benefits of this design is that:
 - Enables branded types as we can store the type and brand information on the opaque reference.
 - You can now define a Typescript framework reference without the `Typescript` instance. This is `specta_typescript::define` in the new API.

The value held on the opaque reference is basically stored in an `Arc<Box<dyn Any>>`, however we had to do some special work to make `PartialEq` and `Eq` work.

With this system:
 - `Reference::opaque(5) == Reference::opaque(5)`
 - `Reference::opaque(5) != Reference::opaque(10)`

This works well for all usecases as the previous behavior of `opaque_from_sentinel` can be achieved by implementing an empty struct:

```rust
pub struct Identity;

Reference::opaque(Identity) == Reference::opaque(Identity)
```

One really cool sideeffect of this system is that we can expose `OpaqueReference::type_name` which gives us the full name of the type. This is great because langauge exporter can show errors which give the user much more information as it can point to the exact source of the type.

In the old system we had something like:

```rust
pub struct Any<T = ()>(T);

pub(crate) static ANY_REFERENCE: Reference = Reference::opaque_from_sentinel({
    static SENTINEL: () = ();
    &SENTINEL
});

impl<T> Type for Any<T> {
    fn definition(_: &mut TypeCollection) -> DataType {
        // This just be `Reference::opaque()` but it *must* be cloned and identicial on each call.
        // You could have a `OnceLock<Reference>` which is simple but it would require the memory allocated by the `Reference::opaque()` call to exist for the lifetime of the program.
        // Using `Reference::opaque_from_sentinel` doesn't allocate a reference but is also a lot more error prone. Eg. using `const` instead of `static` will break it's invariants.

        DataType::Reference(ANY_REFERENCE.clone())
    }
}
```

Whereas in the new system we can do something like:

```rust
use specta_typescript::define;

pub struct Any<T = ()>(T);

impl<T> Type for Any<T> {
    fn definition(_: &mut TypeCollection) -> DataType {
        DataType::Reference(define("any"))
    }
}
```

Much simplier. To explain in some more detail:

`define` is implemented as such:

```rust
#[derive(PartialEq, Eq, Hash)]
pub(crate) struct Define(pub(crate) Cow<'static, str>);

pub fn define(raw: impl Into<Cow<'static, str>>) -> Reference {
    Reference::opaque(Define(raw.into()))
}
```

and then in the Typescript exporter reference generation logic we do:

```rust
fn reference_opaque_dt(
    s: &mut String,
    exporter: &Exporter,
    types: &TypeCollection,
    r: &OpaqueReference,
) -> Result<(), Error> {
    if let Some(def) = r.downcast_ref::<opaque::Define>() {
        s.push_str(&def.0); // pushes the Typescript straight to the output
        return Ok(());
    }

    ...
}
```

### Branded types prototype

This is unfinished but I have a working prototype of Typescript branded types. If you haven't come across branded types before, [checkout this Effect article](https://effect.website/docs/code-style/branded-types/).

Currently the API looks like the following:

```rust
use specta_typescript::branded;

branded!(#[derive(Default)] struct AccountId(String) as "accountId");

#[derive(Type)]
pub struct Account {
    id: AccountId,
    name: String,
}
```

<Callout title="This API is experimental" type="warning">
    I am happy with the low-level implementation ontop of opaque references but I quickly threw together the user-facing API for testing.

    I think the `branded` macro_rules will probably stay but I can't say for sure and whether it's syntax will change or not.
</Callout>

The API:
 - Can only have a tuple struct with a single field which is inherently transparent (Eg. we export as `String` not `AccountId` in the previous example)
 - Works with generics
 - Allows omitting `as "brand"` and it will fallback to the struct ident (Eg. `AccountId` in the previous example)

The way this is implemented under the hood is using an opaque reference:

[The implementation is here](https://github.com/specta-rs/specta/blob/main/specta-typescript/src/branded.rs) but it looks remarkably similar to `define` which is shown above.

## Review Tauri integration

Currently we have a small amount of Specta code within Tauri. This is what powers the integration with Tauri Specta and TauRPC. I had a look at each part of it to see if we can improve it.

### Reexports

Tauri has a built-in Specta feature to provide implementations for `Type` and `specta::function::*`. These were required to workaround the fact that Tauri v1 and v2 couldn't exist as a dependency within the same Cargo workspace ([explained here](https://github.com/specta-rs/tauri2)) which was causing a massive issue in Tauri Specta. These implementations being upstreamed meant this was not going to be a problem for a potential Tauri v3 or later.

The downside with this is that Tauri is a semver-stable library so it's dependency on Specta needs to be taken carefully to ensure a downstream user doesn't get their project broken by a `cargo update`.

I was able to remove `pub use datatype::DataType;` from Specta's root as Tauri doesn't depend on this and it was just left in caution.

Long term sadly, we have to keep `pub type TypeMap = TypeCollection;` as Tauri imports from `function::TypeMap` and it would be breaking to remove.

### Channel

This is used by both Tauri Specta and TauRPC to ensure a `tauri::ipc::Channel` in a command can be typed correctly. This is more complicated than a regular type because we need to import `Channel` from `@tauri-apps/api/core` as it's not a type Specta creates.

Under the hood the way this worked in the past was we did:

```rs
self.types
    .remove(<tauri::ipc::Channel<()> as specta::NamedType>::sid()); // `NamedType` changed a lot across versions but concept is the same
```

and then we always included the `import { Channel } from "@tauri-apps/api/core";` in the Typescript file. This was a great hack to get this feature working but is not something great as a long term solution. This hack meant that the `Channel` import would always exist in the bindings file, even when not used and also we broke an invariant of Specta system, that a reference can't exist without a type being created in the `TypeCollection`.

With the changes on `main` I had previously removed the `TypeCollection::remove` method which renders this hack impossible so it was time to finally figure out a proper solution.

Given i've been working with opaque references my first thought was to try them. What if we had something like the following in Tauri:

```rust
impl<T> Type for Channel<T> {
    fn definition(_: &mut TypeCollection) -> DataType {
        pub struct Channel;
        DataType::Reference(Reference::opaque(Channel))
    }
}
```

This makes heaps of sense but how does the opaque reference becomes some Typescript code?

Well we get two options:
 - Make `tauri` depend on `specta_typescript`, or
 - Make `specta_typescript` depend on `tauri`

We either need to call `Channel::definition` in the `specta_typescript` so we can `if incoming_ref == get_ref(Channel::definition()) { ... }` to branch on the channel or we need to use `specta_typescript::define` in Tauri. Neither of these are a viable option for semver reasons so we need to look somewhere else.

Using opaque references would also require a major breaking change in Tauri which is would not be viable. This systems means we only need this small upstream change ([tauri-apps/tauri#14812](https://github.com/tauri-apps/tauri/pull/14812))  to drop `#[specta(rename = ...)]` from `Channel`.

Instead inside of Specta we patch over the Tauri channel type with the following taken from `named.rs`:

```rust
// Called by `Type` macro inside the `fn definition` function.
fn init_with_sentinel(...) {
    let ndt: NamedDataType = ...;

    // We patch the Tauri `Type` implementation.
    if ndt.name() == "TAURI_CHANNEL" && ndt.module_path().starts_with("tauri::") {
        // This produces `never`.
        // It's expected a framework replaces this with it's own setup.
        ndt.inner = Infallible::definition(types);

        // This ensures that we never create a `export type Channel`,
        // instead the definition gets inlined into each callsite.
        ndt.inline = true;
    }
}
```

These two changes mean that any `Channel`'s export as `never` by default. Well how do we transform this into a `Channel` import? This is where Tauri Specta/TauRPC come in with the following:

```rust
use specta_typescript::define;

let types: TypeCollection = ...;

// We replace the `never` with Typescript string `Channel`
types.iter_mut(|ndt| {
    if ndt.name() == "TAURI_CHANNEL" && ndt.module_path().starts_with("tauri::") {
        *ndt.ty_mut() = DataType::Reference(define("Channel"));
    }
});

// We can also then detect when the `Channel` import is required by crawling the `DataType` for each command.
let is_channel_used = commands.iter().any(|command| {
    // Check if any argument is a Channel
    command.args().iter().any(|(_, dt)| is_channel_type(dt, &cfg.types))
        // Check if result contains a Channel
        || command.result().is_none_or(|result| match result {
            FunctionReturnType::Value(dt) => is_channel_type(dt, &cfg.types),
            FunctionReturnType::Result(ok, err) =>
                is_channel_type(ok, &cfg.types) || is_channel_type(err, &cfg.types),
        })
});

if is_channel_used {
    output.push_str("import { Channel } from \"@tauri-apps/api/core\"");
}
```

This implementation works but in the bindings file you end up with the following unexpected type:

```rust
export type Channel<TSend> = Channel;
```

This is because Specta generates a type for every named type. If you think about it through the `Channel` named type is inlined. This means the exporter will never generate `{ property: Channel<T> }` it will always generate it's type inline like `{ property: never }`. Given this we know it's impossible for any inline type to be referenced and therefore it's not necessary to export them.

This also required making some tweaks to the `Reference` system. Namely that for this to work we must ensure all named references are inlined if the container is inlined. This was already how it was intended to work but the implementation overlooked a couple of edgecases which needed fixing.

### Function implementations

Long term I want to remove Specta's function handling system (`specta::function::*` & `specta::specta` macro) as it was a good idea in theory but i'm not sure it's something that belongs in Specta's core anymore. The currently implementation is heavily tied to the way `#[tauri::command]` works and to fix some remaining bugs in Tauri Specta this is going to get worse.

I built a prototype of this in a fresh crate and started work on a PR to Tauri but it's incomplete.

At a core level if Tauri exposes:
 - the names of the command arguments
 - the doc comments on a command
 - the deprecated attribute of a command

Tauri Specta could work without requiring the `#[specta::specta]` macro and without stabilising the `specta::function::*` and `specta::specta` macro.

Although these changes are for Tauri Specta they are being done under the higher-level goal of reducing the API surface of Specta for better maintainability & reliability.

<Callout title="Incomplete work" type="warning">
    This work on removing the function stuff from the core is incomplete but being documented as I did more research into it's viability.
</Callout>

## Restructure `specta_typescript`

Specta Typescript has been in need of a lot of work to properly support frameworks like Tauri Specta and TauRPC with the new `Layout` system for multi-file exporting so we were in need of a major refactor.

Previously the `JSDoc` exporter was defined something like:

```rust
pub struct JSDoc(Typescript);

impl Default for JSDoc {
    fn default() -> Self {
        let ts = Typescript::default();
        ts.jsdoc = true
        ts
    }
}
```

now we have refactored so that both `JSDoc` and `Typescript` wrap an `Exporter` struct which contains the implementation. This is useful because:
 - You can erase a `Typescript` or `JSDoc` into an `Exporter` so more of your export code can be shared.
 - It's clearer how conversions work. Eg. What does converting from a `JSDoc` to a `Typescript` do, should it disable JSDoc or not. Previously it did but that mean you couldn't get a exporter-type which spanned both languages without a generic + trait system.

This also allowed moving some of the more low-level framework APIs from `Typescript` to `Exporter` making them harder to use. The user must explicitly `Export::from(Typescript::default())` to set certain parameter.

The logic in the `Exporter` was rewritten to:
 - be more reliable
 - for `Layout::Files` generate the files before any FS operations so any thrown errors don't leave an invalid state on disk
 - for `Layout::Files` properly cleanup leftover files in the directory (even when converting between `.ts` and `.js` extensions)
 - Check for duplicate type names, modules or import for each logical scope (whether that be a file or a namespace). The previous system only checked for duplicates on `Layout::FlatFile` which is wrong.
 - Handle `Reference::requires_reference` to omit creating a named export for any types which are container inlined (Eg. Tauri Channel) and hence will never be referenced.
 - Factor the code out into helper functions, allowing for more code reuse.
 - Omit creating files or namespaces if the module is empty
 - Restructured `Layout::Namespace` to properly create references across namespaces without shadowing issues.
 - Ensure a consistent type export order in `Layout::Files` or `Layout::Namespaces`. Copied from the `TypeCollection::into_sorted_iter`
 - Convert `framework_runtime` into a function which takes in `FrameworkExporter` giving access to the `TypeCollection` and a way of interspersing user types within the runtime code (instead of it being appended which is the default).
 - When doing `Layout::Files` create `export * as module_name from "./module_name"` exports so that you can easily go `module.module.Type` in Typescript
 - wip: progress towards fixing imports across files with `Layout::Files`

 This has been a lot of work but it is going to be a huge pay of for the ecosystem. Previously a lot of this logic in various forms was in Specta, Tauri Specta and TauRPC. By formalising it and centralising it within Specta all of the frameworks can hand off the maintenance burden to a single well-tested implementation in Specta.

## Upgrade frameworks

Tauri Specta:
 - Rewrite exporting logic around `Exporter`
 - Using `framework_runtime` so that layouts are supported
 - Making `tauri::ipc::Channel` work with the new integration.
 - Improve the bindings format to be much cleaner and hand off more of it's generation to Specta using the new opaque references system
 - Only include parts of the file as required. Eg. imports or runtime functions are detected and omitted if not used.
 - Rethinking error handling based on Typescript type-inference to allow support for EffectTS, neverthrow and other similar methods. We are going to port this to TauRPC in a future phase!

TauRPC: Sadly ran out of time to complete this. Will be first thing in next phase but should be fairly smooth now that Tauri Specta has proven out the flexibility of the new APIs.

Work on Tauri Specta has allowed me to experiment as Tauri Specta has always been a really good testing ground for experimental Specta APIs due to it's design.

## Restucture tests

I rebuilt the testing harness around snapshot testing and testing all permutations of `inline`, `export`, `reference` and also using all `SerdeMode`'s along with testing `export` and `export_to`. All tests run and build up snapshot files using [insta](https://insta.rs).

The move to snapshot testing was to ease manitaince burdon as Typescript types can be represented in many ways which are all valid due to formatting so snapshot testing allows formatting changes to easily be updated across multiple tests.

The new harness also defines types to test separately from the tests themselves. This makes it easy for us to run each type through a heap of permutations of settings without needing a massive amount of code in the tests.

I also spent some time rename and removing clearly duplicate tests.

<Callout title="Work is incomplete" type="warning">
    I have built the new harness and migrated tests however the snapshots being generated do contain bugs. I need to spend some time ensuring the snapshots for things that were previously untested are correct.
</Callout>

## Phase 3

(I had a week off from the 30th Jan to 9th Feb)

From 9/02/2026 to 27/02/2026

## Stability

Focusing on stability and bug fixes. Including:
 - Fix internal/adjacent enums with only Fields::Unit's variants - [591a5f3](https://github.com/specta-rs/specta/commit/591a5f3ddc78348abf4cbb541d599d65306d92b9)
 - Support generics on `tauri::ipc::Channel` (a bit of a oversight missing this 😅) - [253e6cc](https://github.com/specta-rs/specta/commit/253e6cc48e5dfc1974ac64c83dfeca4aaff464d0)
 - Fix issue with ICF and invariant of type identifiers breaking Specta in non-development builds - [#447](https://github.com/specta-rs/specta/pull/447)
 - Fix attribute parsing and lowering which would break `#[derive(thiserror::Error)]` - [11e76f1](https://github.com/specta-rs/specta/commit/11e76f1773854d47377002123e0c50d557c6d47f)
 - Sometimes `DataType::Generic` would be exported when inlining so you might see `T` in contexts where it was never defined - [29a1379](https://github.com/specta-rs/specta/commit/29a1379f97e198a8ab40dafae57927ea13fffa02)
 - Remove `String::leak` which got unintentionally left in the Typescript exporter - [a6fd79c](https://github.com/specta-rs/specta/commit/a6fd79cf011918fb2f070e171d937d91615d11b7)
 - Ensure reexports are rendered even if framework runtime is empty - [f5b7c86](https://github.com/specta-rs/specta/commit/f5b7c86fb0978990d22919e7549fee293e1776c3)
 - `export_internal` should implicitly handle JSDoc instead of it being done externally - [e6c76a7](https://github.com/specta-rs/specta/commit/e6c76a74bfc4a99ea8dbef1dd107f5443dab7457)
 - Don't panic on dangling references in the Typescript exporter - [db9f060](https://github.com/specta-rs/specta/commit/db9f060e51704277d17c15a9940d73f1c388870e)
 - Ensure we don't export empty namespaces (This was a bug due to the filtering of `requires_reference`) - [68804d7](https://github.com/specta-rs/specta/commit/68804d76d2df8110f2f3c1e43fe96b2fc49075f4)
 - Ensure all types on `#[specta(collect = false)]` is on all types in test-suite - [c80a485](https://github.com/specta-rs/specta/commit/c80a485a27cb68c8df9c8772c4c9959601b670ed)
 - Fix padding for namespace exporting as it's inconsistent - [5657070](https://github.com/specta-rs/specta/commit/56570703e62b86539975cc70f6a481e28c8aab2b)
 - Make the FS exporting in `Exporter::export_to` more reliable - [d044b4b](https://github.com/specta-rs/specta/commit/d044b4bdfc90fadb1578bd650c21c58337d80d59)
 - Fix branded types demo as `T & string` is not a proper brand - [29abcf6](https://github.com/specta-rs/specta/commit/29abcf61ae287c9ce262da7a426a34daf7b3494a)
 - Allow user to configure the branded types template (Eg. use `ts-brand` or EffectTS branded types) - [90547b4](https://github.com/specta-rs/specta/commit/90547b4f84aa771307799d557d37fea1b8fc36c2)
 - Bring back `specta_typescript::collect_references` for tracking cross-file imports - [711fe6f](https://github.com/specta-rs/specta/commit/711fe6f7190092d1769e99866f617ca61918a483)
 - Make `Layout::Files` properly generate imports and references for types defined in other files - [24f3469](https://github.com/specta-rs/specta/commit/24f3469c1381bd9356c1d399cfe87dc0adcf4de6)
 - Make all crates and tests compile again - [adb1455](https://github.com/specta-rs/specta/commit/adb1455edc8697b1686c241f8d23ebfb0e28e11e), [d918b63](https://github.com/specta-rs/specta/commit/d918b63616408a4889bf522f7784941d8bb1fcde), [7d574fe](https://github.com/specta-rs/specta/commit/7d574feb4fa5391d95c66f5a654f1e2556970be2)
 - Give proper migration hint in compiler errors on the usage of deprecated `#[specta(...)]` attributes - [a658b1b](https://github.com/specta-rs/specta/commit/a658b1b43837247f7bab89284781b42a993e9fda)
 - Fix exporting of comments and the deprecated attribute [135aa8b](https://github.com/specta-rs/specta/commit/135aa8b1c4058e773573bc8f33e9df18e10a3926)
 - Upgrade all Rust dependencies to solve RUSTSEC issues - [84a7f4a](https://github.com/specta-rs/specta/commit/84a7f4afe87c29b84bc47c0d3f34c31e304348e4)
 - Fix inline behaviour at field and container level as it was not working - [0d868f9](https://github.com/specta-rs/specta/commit/0d868f9202680eb4931b20a488eede177c3f3f46)
 - Improve the formatting of the generate Typescript bindings - [ae567af](https://github.com/specta-rs/specta/commit/ae567af05ef841f9ee44ae7e8cc482f905af2ee9), [393ce96](https://github.com/specta-rs/specta/commit/393ce96a1fe27036db17c034b96c570984e8c545), [5965190](https://github.com/specta-rs/specta/commit/5965190769aab0c8156a5bc484645529bafa76e0)
 - Make `specta_typescript::primitives::export` take `impl Iterator<Item = &NamedDatatype>` for better JSDoc rendering & formatting - [1b14234](https://github.com/specta-rs/specta/commit/1b1423482244e9cdf6fa3b03fa037ed26115d5f8), [9ea28b8](https://github.com/specta-rs/specta/commit/9ea28b83b7a81ca18ecaaeea9cb357262238d0f6)
 - Ensure JSDoc exporter does imports correctly - [697fe23](https://github.com/specta-rs/specta/commit/697fe238c4bb7da54afec972fc2f63d46096f32b), [2127c99](https://github.com/specta-rs/specta/commit/2127c99a48ffbfe566ab599a3ca2a779a177a6de)
 - Fix Cargo warning about multiple examples with the same name conflicting - [af55947](https://github.com/specta-rs/specta/commit/af55947669b4d186febbf19b665295b346bba06e)
 - Fix `unreachable!()` when constructing enums using builder API - [c7f1dc3](https://github.com/specta-rs/specta/commit/c7f1dc3cb105a8334fe64433997d67ccee5e3fe7)
 - Cleanup workspace lints, fix Clippy warnings and ensure all public types have docs - [e2f4f9f](https://github.com/specta-rs/specta/commit/e2f4f9fc509aa6248dbc4b22e1ecd25accac0ca6), [14a4ee8](https://github.com/specta-rs/specta/commit/14a4ee845c0a45169bc622dad2edbbd0d22cbf97)
 - Migrate final tests from the old test suite to the new one - [0ea4c6c](https://github.com/specta-rs/specta/commit/0ea4c6cfcee4874a05e8c08ffbcda166c08d21c7)
 - Make the `framework.rs` example emit into a `.gitignore`'d directory - [68b8dc7](https://github.com/specta-rs/specta/commit/68b8dc72eeb84c9ada214c03241fe06287a14dc0)
 - Fix `Error::InvalidUsageOfSkip` issue in the Serde checking logic - [4b59e77](https://github.com/specta-rs/specta/commit/4b59e77a3e6e6bb2fcdbd919bb7cb45b06aeaa89)
 - Fix bugs with variant inline & tuple exporting when using Serde transformer - [3e64c30](https://github.com/specta-rs/specta/commit/3e64c30d40f49ddfe0a478e270f2496cf1196563)
 - Fix issues with Serde tagging on enums & JSDoc formatting issues with comments in comments - [345b9fe](https://github.com/specta-rs/specta/commit/345b9fe6a7a33e9ec91cf57922001515f5bc8075)
 - Remove `is_export` from Specta Typescript internals. This means comments are exported when inlining - required for Tauri Specta/TauRPC - [7396d94](https://github.com/specta-rs/specta/commit/7396d94ccbb7fd4ffff5f2d363ad0580faf6cc40)
 - Deterministic order of snapshot test results - [b25ec77](https://github.com/specta-rs/specta/pull/452/changes/b25ec77cc6f66f4a036dff837fdf2fc3be1d0acb)
 - Support `#[specta(type = ...)]` on container (used in one of TauRPC's examples) - [#452](https://github.com/specta-rs/specta/pull/452)
 - Fix object key escaping. An empty field name would be exported as `: {}` which is not valid - [d874898](https://github.com/specta-rs/specta/commit/d874898caec267a051b473113b18f5f9f389a644) & [9bbab84](https://github.com/specta-rs/specta/commit/9bbab84e0ff3d92de47d79d2e0016595ca88fded)
 - Setup dependabot with PR grouping for keeping dependencies updated - [6be7a76](https://github.com/specta-rs/specta/commit/6be7a76fd85a3efedc33227fc7bf5d8735600ef0)
 - seal Typescript error type - closes [#446](https://github.com/specta-rs/specta/issues/446) - [22cc21e](https://github.com/specta-rs/specta/commit/22cc21ef54521374c888c6ce907768b0a72f4b62)
 - Fix `null | null` being the result of `Option<()>` - [f4ea005](https://github.com/specta-rs/specta/commit/f4ea005a9217731b316aeb6f89e2f4c578fb1420)
 - Allow tracking the source of `specta_typescript::Error::framework` - [688100e](https://github.com/specta-rs/specta/commit/688100ea7f1dee064a0de2b8692ec5c77f348027)
 - Dyn-safe callbacks for branded types and runtime generation (required by TauRPC) - [7206c6c](https://github.com/specta-rs/specta/commit/7206c6c9ec6c31d21ea813c6040f3f923bae78b7) & [f3988f6](https://github.com/specta-rs/specta/commit/f3988f6a513cfbf72480bffbdcf4259775416974)
 - Allow branded type exporter to properly render datatypes with access to `TypeCollection` and local `Typescript` instance - [955019d](https://github.com/specta-rs/specta/commit/955019d52c241c18a98c2847bb5e584d15080c97)
 - Protect against duplicate field names - closes [#330](https://github.com/specta-rs/specta/issues/330)
 - Review transparent and inlining implementation - closes [#313](https://github.com/specta-rs/specta/issues/313) & closes [#311](https://github.com/specta-rs/specta/issues/311)
 - Decided it's a bad idea to implement `impl_for!` - closes [#424](https://github.com/specta-rs/specta/issues/424)
 - Renders comments in single line if possible for Typescript exporter - closes [#450](https://github.com/specta-rs/specta/issues/450) - [3dd634d](https://github.com/specta-rs/specta/commit/3dd634d7d90057eb89caf1411c8277a446c7c94c)
 - Closed some issues as completed, duplicate or not planned - [#361](https://github.com/specta-rs/specta/issues/361), [#357](https://github.com/specta-rs/specta/issues/357), [#346](https://github.com/specta-rs/specta/issues/346), [#336](https://github.com/specta-rs/specta/issues/336), [#258](https://github.com/specta-rs/specta/issues/258), [#193](https://github.com/specta-rs/specta/issues/193), [#187](https://github.com/specta-rs/specta/issues/187), [#145](https://github.com/specta-rs/specta/issues/145), [#302](https://github.com/specta-rs/specta/issues/302)

 Although bugs still remain I have been going through each of the snapshots generated by the new test suite and comparing each result to the legacy tests to ensure they are valid. This has led to fixing a lot of bugs.

 I will keep focusing on this until I feel confident we don't have a lot of remaining issues and will cut a Specta release.

## MaybeUndefined

I did an investigation into what it would look like to have official support for a `MaybeUndefined` type in Specta as i've had issues with this in the past.

It's very common to want a field which accepts `null` (empty value), `undefined` (unchanged value) or `T` (new value). For example for a endpoint to update the user. `undefined` is treated as no update required, while `null` will empty the value in the field and set `NULL` in the database.

It looks something like the following:

```rust
#[specta(untagged)]
pub enum MaybeUndefined<T> {
    Undefined,
    Null,
    Value(T),
}
```

From my research this is not something I see a clear way of supporting in Specta as it comes with a lot of issues:
 - Not using `#[serde(default, skip_serializing_if = "MaybeUndefined::is_undefined")]` on *every* field using `MaybeUndefined` will result in unexpected behavior.
 - `MaybeUndefined::<T>::Null` is serialized as `null` directly, which is confusing. `undefined` is the lack of a field and Serde doesn't understand this inside of a field.
 - `MaybeUndefined::Undefined` is really hard to type because "undefined" is not understood by Specta's core and I think I want to keep it that way.

In the [GitHub Issue #157](https://github.com/specta-rs/specta/issues/157), I have posted a slightly improved example but I don't think I see a way of supporting this without a lot of asterisks so for now it can stay userspace.

(The code in the GitHub issue I shared doesn't actually export the type properly right now but it will when work on per-phase types is completed)

## TauRPC

I have spent some time upgrading TauRPC to work on the latest Specta version. This is more involved than just bumping the Specta version as I have rewritten TauRPC's logic to use Specta's new exporter system. This gives out of the box support for layouts (multi-file exporting or namespaces) and branded types. With this change both Tauri Specta and TauRPC both use Specta's upstream exporter which means we can have a single battle tested implementation shared across the ecosystem. This also gives Specta more control over exporting which enables us to support features which previously would previously have had to be implemented in each framework separately. This PR [#58](https://github.com/MatsDK/TauRPC/pull/58) is ready to be tried out!

I have also been investigating type-safe error handling. As TauRPC uses the `specta::specta` macro and parses around `specta::datatype::Function` struct's we already have the result information available which is nice. I have implemented a prototype on [#62](https://github.com/MatsDK/TauRPC/pull/62) which I will pick up again once [#58](https://github.com/MatsDK/TauRPC/pull/58) is merged.

## Real-world testing

### The new attribute parsing

The fact that we have decoupled Serde from the attribute parsing is actually proving to have had a lot of really cool side-effects.

For example the following:
```rs
#[derive(Type)] // - notice no `Serialize`/`Deserialize`
#[serde(untagged)]
pub struct Foo {
    a: String,
} // doesn't compile
```

This used to be valid as the Specta attribute "advertised" it could handle `#[serde(...)]`, but we don't actually require that so it's been removed. Now you will be required to add `#[derive(Serialize)]` (and/or `Deserialize`) as that is required for it to work on your type at runtime.

We used to support the following:
```rs
#[derive(Type, Serialize)]
#[specta(untagged)] // - notice `specta` not `serde`
pub struct Foo {
    a: String,
} // doesn't compile
```

Now it's no longer supported as `#[specta(untagged)]` can't apply `#[serde(untagged)]` where the latter only effects the type, and the latter effects the types and runtime behavior.

The macros will warn for any previously supported attributes which should help make the migration smooth!

### Runtime inlining is cool

The most recent work I did before starting with Flight Science was to move inlining from the macro to runtime.

I originally did this inlining in the macro could cause stack overflows as a type would call `<T as Type>::definition(...)` on a type which would then call it again on itself. This was especially a problem for the `export` (now called `collect`) feature runs which uses [`ctor`](https://docs.rs/ctor) and where [stack overflows cause a segfault 😭](https://github.com/mmastrac/rust-ctor/issues/159#issuecomment-1712866524).

This had a cool implication, when using `#[serde(inline)]` which cause a type definition to be "flattened" in the past we could only flatten a single level as it was done in the macro. Now we can be much smarter and flatten through transparent types like `Option` and `Box`.

In practice this means:
```rs
#[derive(Type)]
pub struct Foo {
    #[specta(inline)]
    a: Option<Bar>,
}

#[derive(Type)]
pub struct Bar {
    b: i32,
}
```

Now becomes:

```rs
export type Foo = {
  a: {
      b: number;
  } | null
};
```

Where previously it was:

```rs
export type Foo = {
  a: Bar | null
};
```

The old behavior is no difference from omitting the `#[specta(inline)]` as it couldn't project through the `Option`.

### Production usage!

[OpenCode](https://opencode.ai) briefly shipped with the Specta `main` branch. Brendan who works on their team helped in the original Specta and Tauri Specta development so I worked with him of fixing some bugs to get it working.

They are migrating to Electron through so this is short lived.

This means we are really close from getting this stuff running on Flight Science's codebase to check for regressions.

## Phase 4

From 27/02/2026 to 13/03/2026

### Finishing up from last sprint

Some remaining issues carried over from the last sprint that where blocking a new release:

 - Fix final known failing tests - [fe39669](https://github.com/specta-rs/specta/commit/fe396690e078ad7213ffef0b754715cda760b80a) & [ee2352d](https://github.com/specta-rs/specta/commit/ee2352d69eb5daf6dd88cb6ca03a14d9a66a0faf) & [429bdd4](https://github.com/specta-rs/specta/commit/429bdd4b9173a0f36a5b4051829e37fd2df74294)
 - Fix `f16` and `f128` implementation on nightly - [1257bb4](https://github.com/specta-rs/specta/commit/1257bb4867f5266b8b9492dabfb5ff23d52712c0)
 - Fix CI workflows. The snapshots generated differently on Windows so this took some changes to it's format. - [f3143fe](https://github.com/specta-rs/specta/commit/f3143fe210ce483b983d413e516914da990c6bbc) & [ac74b0a](https://github.com/specta-rs/specta/commit/ac74b0ad45e5ea99c123d51e4f778a073b2e3c0d) & [0f501b9](https://github.com/specta-rs/specta/commit/0f501b991e46298625fb397b46ea808d3d693d27)
 - Add alpha warning to all non-typescript crates ahead of publishing them for the first time - [0c69ad9](https://github.com/specta-rs/specta/commit/0c69ad959c7617a8ebac1f490629346fa71cc73c)

Cut release [2.0.0-rc.23](https://github.com/specta-rs/specta/releases/tag/v2.0.0-rc.23)

I have been working with MatsDK to get TauRPC upgraded but [#58](https://github.com/MatsDK/TauRPC/pull/58) is ready to go!

### Stricter enums

Specta's Typescript exporter has historically been too permissive for certain enum shapes. TypeScript unions are inclusive, so structurally-compatible variants could be accepted incorrectly. For example given the following type:

```rust
enum Example {
    A { a: String },
    B { b: String },
}
```

The following shows the difference:

```rust
// Specta's previous output. Notice we can parse both `a` and `b` which is incorrect.
type Example = { a: string } | { b: string };
const _: Example = { a: "aaaa", b: "bbbb" };

// Specta's new output
type Example2 = ({ a: string }) & { b?: never } | ({ b: string }) & { a?: never };

// This yields a Typescript error as expected:
const __: Example2 = { a: "aaaa", b: "bbbb" };
```

This was fixed in [4606a42](https://github.com/specta-rs/specta/commit/4606a42) (closes [#221](https://github.com/specta-rs/specta/issues/221)).

## `Result<T, E>`

I investigated adding a `Type` implementation for `Result<T, E>`. We previously removed this as it conflicted with our result handling implementation for `specta::function::FunctionResult` but with a lot of the work we've done in the core it's now possible to support this!

### Regig `FunctionResult`

The first step was to remove the `impl<T, E> FunctionResult for Result<T, E>` as theoretically we can now rely on `impl<T: Type> FunctionResult for T` so this was easy but did require a little bit of cleanup to remove [`FunctionReturnType`](https://docs.rs/specta/2.0.0-rc.23/specta/datatype/enum.FunctionReturnType.html).

Now this is actually enough to have a working `Result` implementation in Specta's core but downstream frameworks like Tauri Specta need a way of being able to determine if a `Result` is a success or failure for generating helper code so this isn't enough!

### Everything is an NDT

Previously for `impl Type for String` Specta would return something like `DataType::Primitive(PrimitiveType::String)`. What if we rethink that?

First of all `String` is not actually a primitive, `str` is the primitive so the first step was renaming `PrimitiveType::String` to `Primtive::str`. We also were able to cleanup some of the implementations by including a `impl Type for str` and then relaxing the `?Sized` bounds on some other implementations, such as those for containers such as `Box`, `Arc`, etc.

Now that we have a set of primitives `str`, `[T]`, `i32`, etc we replaced the `Type` implementation for non-primtives to return a reference to a `NamedDataType` which we collect up into the `TypeCollection`. These are inlined by default so as a user you never actually see anything different but internally the exporter such as Typescript can see these named types to know more about what's going on.

All of these `NamedDataType` implementations dispatch their `Type` implementation to a primitive. This is done to avoid `NamedDataType` wrapping. You don't want a `Date` to return a reference to a `String` which then has to be looked up to be find out it points to a `PrimitiveType::str`, you want to avoid the middleman. For example `IndexMap<T, E>` used to just call `<HashMap<T, E> as Type>::definition(...)` internally but that would result in these two named types being nested. Now we have a `MapPrimitive` internally that is used by both implementations to avoid onioning.

This comes with such a massive improvements in that now any language exporter or tool can properly trace the `DataType` layer. For example, if we want to have a Rust exporter we could use the informations from the `NamedDataType`'s to determine the difference between an `IndexSet` and a `HashSet` where previously they would only be known as a `DataType::List` with more specific type information attached.

### Generics are hard

This change broke the handling of generics majorly. Namely when defining a `NamedDataType` in Specta you need to be very careful to ensure that you define generics on the `NamedDataType` and ensure the inner `DataType` only contains `DataType::Generic` slots which the language exporter can fill in.

We also had issues in the Typescript exporter with resolving generics that where multiple types deep so I decided to rethink the generics a little bit.

We deleted `DataType::Generic` and replaced it with `Reference::Generic` along with giving each generic a unique identity which helps to properly track them in the exporter. This also provides much harder errors for when the langauge exporter is unable to properly export the generics as it is able to properly check the generics exist in the current scope.

### Derive be gone

To fix all these generic issues I revampped the internal `macro_rules!` used for defining `impl Type for X`. With these changes a lucky side-effect is that we no longer require `specta-macros` for any of the built-in implementation. In limited conditions this could provide a compile time benefit and makes things cleaner.

### Conclusion

Merged in [#454](https://github.com/specta-rs/specta/pull/454), closes [#415](https://github.com/specta-rs/specta/issues/415), [#349](https://github.com/specta-rs/specta/issues/349), [#455](https://github.com/specta-rs/specta/issues/455)

## Serde integration needs some serious TLC

The `specta-serde` crate is a bit a mess at the moment so I sat down to try and sort it out and also support the remaning `#[serde(...)]` attributes which we don't currently support.

### How it works?

For the types Specta generates to properly reflect how Serde serialized and deserialized them we need to be able to adjust the types based on the presence of `#[serde(...)]` attributes. For example `#[serde(rename = "new_name")]` should rename the field to `new_name` in the generated type.

For a long time we did this directly in Specta Macros which would parse these attributes and just make changes to the `DataType` layer directly before the macro generates it. Although this works well it requires each type to only have one singular representation. This means if you want to use a non-Serde format your out of luck and even more importantly if you want to differentiate between the `Serialize` and `Deserialize` implementations your out of luck.

This is where the idea started for making Specta format-agnostic and removing Serde from the core where possible.

The original system for this was introduced during "Phase 1" of my Flight Science work which was a runtime parser for attributes in the `specta-serde` crate. The Specta Macros crate we would parse all macro attributes `#[...]`, for example `#[doc = "My Rust docs"]`, `#[error(...)]` (from `thiserror`), `#[serde(...)]` and more and we would lower them into a [`Vec<specta::datatype::RuntimeAttribute>`](https://docs.rs/specta/2.0.0-rc.23/specta/datatype/struct.RuntimeAttribute.html) structure which could then be processed at runtime. The `specta-serde` crate could pull these attributes from the container, variants and fields and then make the corresponding adjusts to the `DataType` structure for the Typescript exporter to know how to export them.

This worked well for a while, until...

### Approach is flawed

I started trying to implement [`#[specta(into = ...)]`](https://serde.rs/container-attrs.html#into) which seems like a fairly simple feature in theory but turns out it's completely not possible under the previous system.

The issue you run into is that for `#[specta(into = "String")]` we want to call `<String as specta::Type>::definition(...)` to figure out the `DataType` for it, but the attribute parser is within the `specta-serde` crates runtime so it has no way to emit custom code like this. We need the attribute parser to go back to being done at compile time so we can emit custom code like this for the compiler to resolve prior to running the program.

I did a heap of research around this and generated [this plan](https://hackmd.io/@oscartbeaumont/rJe_8avYWl) for solving it.

### A solution enters the chat

What if every format crate exports a `parser!` macro. This would take in something like:
 - `parser!(@container; #[serde(...)]`
 - `parser!(@container; #[doc = "Rustdocs"]`
 - `parser!(@container; #[error(...)]`
 - `parser!(@variant; #[serde(...)]`
 - `parser!(@field; #[serde(...)]`

This macro would be able to parse any incoming attribute it cares about, and also generate code such as the required `<T as specta::Type>::definition(...)` for `#[specta(into = ...)]`. The macro returns an arbitrary struct which it stored in an `Attributes` container which is basically a container for any type (more or less a `HashMap<TypeId, Arc<dyn Any>>`). Now the macro can pass the structures and we can `Attributes::downcast::<SerdeAttributes()` to get access to them at runtime to apply them to the `DataType` layer.

This perfectly solves the problem we originally had and allows us to stay format agnostic while also supporting all of the Serde attributes.

### Tradeoffs

One downside is that Specta's macro crate needs to know about all of the supported formats to know which crates to call `{crate}::parser!` on. Using [`proc-macro-crate`](https://docs.rs/proc-macro-crate) we can detect known formats like `specta_serde` being in the users dependencies to know whether we need to emit the `parser!` call. For custom formats you can set a `SPECTA_FORMAT_CRATES` envrionment variable from your `build.rs`. This is clunky but in practice we would add support for as many formats officially as possible!

### Phase-specific handling

So all this work is cool and all but splitting Serde out of the core doesn't matter to me because I use Serde right?

Actually no,

By splitting it out of the core we can allow the Serde runtime to handle the different phases specifically `Serialize` and `Deserialize` differently.

### The public API?

With this refactor we now have the following API:
 - `specta_serde::apply(types: specta::Types) -> Result<specta::ResolvedTypes, specta_serde::Error>`
 - `specta_serde::apply_phases(types: specta::Types) -> Result<specta::ResolvedTypes, specta_serde::Error>`

Note that both of these return `ResolvedTypes`. This is just a specially named wrapper around the existing `Types` struct, however this is used to indicate it has been processed by a specific-format, in this case Serde.

Specta Typescript and other language exporters will expect `ResolvedTypes` instead of `Types`. This will at the type-level remind the user they must use `specta_serde::apply` or `specta_serde::apply_phases` before exporting the types.

With this Specta Typescript no longer depends on Specta Serde. The two components are completely decoupled.

### What is `apply_phases`?

So `specta_serde::apply` is the old API your used to, except that the presence of phase-specific attributes like `#[serde(skip_serializing_if = ...)]` now throw as an error as it is impossible for a single type to be generated.

`specta_serde::apply_phases` looks through each of your types for phase-specific properties and if they are found it split the type into the following:
 - `MyType` which is replaced with `MyType_Serialize | MyType_Deserialize`.
 - `MyType_Serialize` - which is the version with serialize attributes applied
 - `MyType_Deserialize` - which is the version with deserialize attributes applied

It also traces all depent types and may split them automatically so we can dispatch to the proper phase type.

This all means a framework like TauRPC and Tauri Specta is now able to be much smarter about the types it generates.

### All the Serde atttibutes!

Following this I have worked through supporting as many Serde attributes as possible across all phases as previously we only really had the import ones supported. Although I suspect i'm not yet at 100% support we definitely have much better support.

Attribute coverage expanded significantly during this phase:
 - `variant_identifier` / `field_identifier` - [aba3540](https://github.com/specta-rs/specta/commit/aba3540)
 - `into` / `from` / `try_from` - [8ec2663](https://github.com/specta-rs/specta/commit/8ec2663)
 - `variant #[specta(type = ...)]` + broader attribute support cleanup - [e9a592d](https://github.com/specta-rs/specta/commit/e9a592d)
 - container `type = ...` inference fix and `serde-repr` example - [4ff1c2a](https://github.com/specta-rs/specta/commit/4ff1c2a)

### `specta_serde::Phased`

It is very useful for the user to be able to override the type per-phase so I have introduced `Phased`. It can be used like the following:

```rust
#[derive(Type)]
struct OneOrMany {
    #[specta(type = specta_serde::Phased<Vec<String>, String>)]
    value: Vec<String>,
}
```

This is really cool because it is a way of supporting something like `#[specta(type(serialize = ..., deserialize = ...))]` without needing the core to be aware of Serde phases which would result in a much more fragile and complicated solution.

### Notes about attributes

 - `#[serde(with = ...)]` requires `#[specta(type = ...)]` annotation to work as we can't infer the type.
 - `serde-with` crate doesn't require a special support as it just emits `#[serde(with = ...)]` internally, which Specta can see.

## Other stuff

### Issue count down, down and staying down

I have also spent quite a bit of work going through GitHub issues and fixing them. We are currently at 27 open PRs. when stating this contract we were at *100*. That's absolutely huge!!!!!

TODO:
 - Added parity with crates supported by `ts-rs` - [#454](https://github.com/specta-rs/specta/pull/454) & [2b3aafa](https://github.com/specta-rs/specta/commit/2b3aafa)
 - Add `error-stack` support - [e33a2d2](https://github.com/specta-rs/specta/commit/e33a2d2) (closes [#249](https://github.com/specta-rs/specta/issues/249))
 - Review hidden API surface and docs - [9a13b7e](https://github.com/specta-rs/specta/commit/9a13b7e) (closes [#325](https://github.com/specta-rs/specta/issues/325))

### Zod exporter

I have started working on a Zod exporter. It is still incomplete so will share more in the near future!

[#461](https://github.com/specta-rs/specta/pull/461)

### Nuanced types

This work is still shifting but I think we can fully achieve support for this feature without it existing in Specta's core.

I suspect I am going to close [#419](https://github.com/specta-rs/specta/pull/419) in favor of [#458](https://github.com/specta-rs/specta/pull/458) for this reason.

I have some prototypes running in Tauri Specta but making this something general enough for TauRPC to adopt is still going to require a bit of thought.

## Phase 5

From 13/03/2026 to 27/03/2026

This has been a pretty slow week as I have had a bunch of personal stuff getting in the way.

## Fixing tests

I spent a bunch of time fixing the test suite. We had a handful of regressions from previous work that required a bunch of debugging. All fixes merged in [#463](https://github.com/specta-rs/specta/pull/463).

I also opened [hashintel/hash#8579](https://github.com/hashintel/hash/issues/8579) to fix the issues I have been having with generating documentation with the Rust nightly toolchain due to `error-stack`.

Also closed [#352](https://github.com/specta-rs/specta/issues/352) by cleaning up `specta::internal`.

## Enum discrimination cleanup

In Phase 4 we introduced stricter enum validation and I noticed that the implementation has some edge cases with certain Serde tagging conditions so I review and improved them.

Basically it all boils down to the following which goes through how each tagging condition should be exported:

```ts
// For internally tagged the tag acts as the differentiator so we don't need to do anything special
type A = { type: "A"} | { type: "B", data: string };
const a: A = { "type": "A" };
const b: A = { "type": "B", data: "todo" };
const c: A = { "type": "A", data: "todo" }; // type error

// For adjacently tagged the tag acts as the differentiator so we don't need to do anything special
type C = { type: "A", data: { data: string } } | { type: "B", data: { another: number }  };
const g: C = { type: "A", data: { data: "string" } };
const h: C = { type: "B", data: { another: 42 } };
const i: C = { type: "A", data: { data: "string", another: 42 } }; // type error

// For untagged we need to add the `?: never` fields so both variants can't be initiated at once.
type B = { data: string, another?: never } | { another: number, data?: never };
const d: B = { another: 42 };
const e: B = { data: "bruh" };
const f: B = { data: "bruh", another: 42 }; // type error

// For externally tagged we need to add the `?: never` fields so both variants can't be initiated at once.
type D = { A: { data: string }, B?: never } | { B: { another: number }, A?: never };
const j: D = { A: { data: "bruh" } };
const k: D = { B: { another: 42 } };
const l: D = { A: { data: "bruh" }, B: { another: 42 } }; // type error
```

[ts playground](https://www.typescriptlang.org/play/?ssl=7&ssc=8&pln=7&pc=18#)

implemented in [9e782e4](https://github.com/specta-rs/specta/commit/9e782e4d89a2856968752196178b018366474534) and [331d7c0](https://github.com/specta-rs/specta/commit/331d7c0cefc4da7bb57f4d45c5c1a41db32d38b8)

## Issues with format system

In working on upgrading Tauri Specta and TauRPC I noticed another major issue with our new format system approach. I was getting the following error on a perfectly valid enum:
```bash
thread 'main' (4981415) panicked at examples/app/src-tauri/src/main.rs:155:14:
Failed to export typescript bindings: Framework error: Specta Serde validation failed: Invalid externally tagged enum variant 'IoError': variant payload is fully skipped
```

In looking into it the type is actually valid so what's happening?

Well `specta-serde` isn't a dependency on the example crate (representing the end-users application) which I was compiling. Due to this when `#[derive(Type)]` runs on it, the `#[serde(...)]` attributes are never collected, and hence the Serde transformation never runs and injects the expected `#[specta(tag = ..., content = ...)` which made this type valid.

This is a another oversight on the implementation of formats.

### Dynamic formats is not possible

Conclusion: As much as the experiment has been worth it for providing the foundation of phase-specific types the whole vision of formats like Serde being modular just isn't possible.

The core issue is that the macro runs across types in the entire crate-graph, so we need to collect up format information from all of them, however we can't import the format crate from all crates in the graph (as it had to have been a preexisting dependency) so you end up with incomplete attribute information and invalid and wierd types.

Going forwards:
 - `specta::datatype::Atributes` is now `HashMap<String, Box<dyn Any>>` so we can store attributes in a form like `serde:rename = "new_name"`. This means we don't need to import `specta_serde` to construct it's attributes.
 - `specta_serde::parser!`'s logic is moved into `specta-macros` which now has a non-default `serde` feature which enables the collection of the Serde attributes (this is enabled if `specta_serde` is anywhere in the crate graph)

This makes the Serde integration work really well and still keeps it agnostic from the `specta` crate, however the `specta-macros` crate can't get away without bundling support for it. If we want to support a non-Serde formats we would need to introduce them into `specta-macros` which is worse than the original plan to allow any 3rd party crate to define it, but this is overall still a solid final outcome and is the best we can do with the technical constraints.

Fix in [#466](https://github.com/specta-rs/specta/pull/466)

## Tauri Specta phase-specific handling

I have integerated the work on phase-specific handling into Tauri Specta to confirm the API surface is actually working. I will follow this up with a PR to TauRPC in the next sprint once the current upgrade Specta PR is merged.

From this command and struct definition in Rust:

```rust
#[derive(Serialize, Deserialize, Type)]
struct PhaseSpecificRename {
    #[serde(rename(serialize = "serialized_value", deserialize = "deserialized_value"))]
    value: String,
}

#[tauri::command]
#[specta::specta]
fn phase_specific_rename(input: PhaseSpecificRename) -> PhaseSpecificRename {
    input
}
```

This results in the following in the `bindings.ts` file:

```ts
// ...

export type PhaseSpecificRename = PhaseSpecificRename_Serialize | PhaseSpecificRename_Deserialize;

export type PhaseSpecificRename_Deserialize = {
	deserialized_value: string,
};

export type PhaseSpecificRename_Serialize = {
	serialized_value: string,
};

// ...

export const commands = {
    // Notice how we specialise the types for the usage!
	phaseSpecificRename: (input: PhaseSpecificRename_Deserialize) => __TAURI_INVOKE<PhaseSpecificRename_Serialize>("phase_specific_rename", { input }),
};
```

This means all of the work on `specta-serde` is finally possible to be used! This will result in much more accurate types!

This is being worked on in [specta-rs/tauri-specta#206](https://github.com/specta-rs/tauri-specta/pull/206)

## Nuanced types

I have introduced the `specta-tags` crate. This crate is still a bit of a mess but it's been the playground for properly implementing nuanced types. It's really awesome this can purely be done in userspace with any integration from the `specta` core crate.

### `specta-tags`

When paired with a framework like Tauri Specta (or TauRPC) it can produce something like the following in the `bindings.ts`:

```ts
export const commands = {
  specialTypes: (input: SpecialTypes_Deserialize) => __TAURI_INVOKE<[SpecialTypes_Deserialize, SpecialTypes_Deserialize]>("special_types", { input }).then((v) => [(() => { let __value1 = v[0]; { const next = BigInt(__value1["u128_max"]); if (next !== __value1["u128_max"]) __value1 = { ...__value1, "u128_max": next }; } { const next = BigInt(__value1["u128_min"]); if (next !== __value1["u128_min"]) __value1 = { ...__value1, "u128_min": next }; } { const next = BigInt(__value1["i128_max"]); if (next !== __value1["i128_max"]) __value1 = { ...__value1, "i128_max": next }; } { const next = BigInt(__value1["i128_min"]); if (next !== __value1["i128_min"]) __value1 = { ...__value1, "i128_min": next }; } return __value1; })(), (() => { let __value2 = v[1]; { const next = BigInt(__value2["u128_max"]); if (next !== __value2["u128_max"]) __value2 = { ...__value2, "u128_max": next }; } { const next = BigInt(__value2["u128_min"]); if (next !== __value2["u128_min"]) __value2 = { ...__value2, "u128_min": next }; } { const next = BigInt(__value2["i128_max"]); if (next !== __value2["i128_max"]) __value2 = { ...__value2, "i128_max": next }; } { const next = BigInt(__value2["i128_min"]); if (next !== __value2["i128_min"]) __value2 = { ...__value2, "i128_min": next }; } return __value2; })()]),
};
```

Notice the `.then` logic. This is able to map the fields to their JS types. For example `BigInt(__value1["i128_min"])` in the code is converting the field `i128_min` into a proper bigint type. The same can be done for `Date`'s, `UInt8Array`'s, etc.

The current code being produced is pretty messy but in the future it will likely be much cleaner.

### BigInt's

The `specta-tags` crate handles mapping from the incoming type to a `bigint`, however that is too late as the `JSON.parse` in Tauri already triggers the truncation of the bigint if the number is too large. `JSON.stringify` used for communicating back with Rust always errors on bigint's too so it can't be sent either.

For us to properly support this across Tauri Specta and TauRPC our best bet is to get some form of official support for bigints in Tauri, by how?

#### JS -> Rust

This is as easy as the 3 lines of code changed in [this commit](https://github.com/oscartbeaumont/tauri/commit/169a8ae139c6664595472acb24567077db8cfdd6).

The one major issue I see with this approach is that I don't think [`JSON.rawJSON` is supported](https://caniuse.com/wf-json-raw) on [all platforms Tauri supports](https://v2.tauri.app/reference/webview-versions/).

It can however be polyfilled so that is the likely best option.

#### Rust -> JS

For this to work I tried multiple different approaches. The one I liked the most was allow the user to set a `reviver` on the Tauri `invoke` call. This `reviver` is then pass to the `JSON.parse` parameter.

I implemented this in [e485002](https://github.com/oscartbeaumont/tauri/commit/e485002ae0c5f5afb5286d9846f196306b41ebe1) and [c1d13da](https://github.com/oscartbeaumont/tauri/commit/c1d13daf34ce881e248c06a7bccc31bc1f39afd4) (note these are AI generated and not very carefully reviewed but do work).

The approach does require quite a few changes to Tauri so I definitely would have preferred a simpler solution but I suspect this is our best bet. This solution also has similar browser support concerns but may be harder to pollyfill.

#### Bring them together

This all comes together to make the following possible with the regular Tauri `invoke` API:

```ts
invoke("special_types", {
  input: {
    u128_max: (1n << 128n) - 1n,
    u128_min: 0n,
    i128_max: (1n << 127n) - 1n,
    i128_min: -(1n << 127n),
} }, {
  reviver: (key, value, context) => {
   if (
     context &&
     typeof value === "number" &&
     Number.isInteger(value) &&
     /^-?\d+$/.test(context.source) &&
     !Number.isSafeInteger(value)
   ) return BigInt(context.source);
   return value;
 }
}).then((echo) => {
    console.log(
      "ASSERTIONS:",
      echo.u128_max == (1n << 128n) - 1n,
      echo.u128_min == 0n,
      echo.i128_max == (1n << 127n) - 1n,
      echo.i128_min == -(1n << 127n)
    );
});
```

Then when wrapped with `specta-tags` we end up with support for large integer types without any loss of precion, without any user-defined glue code or changes. Working in both TauRPC and Tauri Specta!

This feature makes your code *just work* without the application developer even needing to be aware of all the issues with large integer types.

#### Removal of `BigIntExportBehavior`

The global `BigIntExportBehavior` configures how large integer types are exported, as by default they will be truncated by JS when deserializing. Although this global option is convenient, it's basically always a footgun to use so in the next release it's finally going to be removed.

With official support for `BigInt`'s in Tauri Specta and TauRPC the need for a global option to disable the error will no longer be required as these frameworks make the error impossible. For people not using a framework, this change will likely be very annoying as it will require overriding every single bigint-style field with an override annotation, or writing very technical Specta logic to do a global override on the type collection. Although this will be annoying it's much more technically correct and will ensure developers are always aware of the danger which `BigIntExportBehavior` previously hide away from them.

The Typescript exporter will always error (as if `BigIntExportBehavior::Fail`) was set and it will have no overridable option to disable that. We will recommend override on a per-field basis but the developer could also generate a transformation function for the `DataType` structure (as this is what frameworks like Tauri Specta and TauRPC will do to surpress the error).

<Callout title="What to about bigint errors?" type="idea">
 Specta Typescript intentionally forbids exporting BigInt-style Rust integer types.
 This includes [usize], [isize], [i64], [u64], [u128], [i128] and [f128].

 This guard exists because `JSON.parse` will truncate large integers to fit into a JavaScript `number` type so we explicitly forbid exporting them.

 If you encounter this error, there are a few common migration paths (in order of preference):

 1. Use a smaller integer types (any of `u8`/`i8`/`u16`/`i16`/`u32`/`i32`/`f64`).
    - Only possible when the biggest integer you need to represent is small enough to be represented by a `number` in JS.
    - This approach forces your application code to handle overflow/underflow values explicitly
    - Downside is that it can introduce annoying glue code and doesn't actually work if your need large values.

 2. Serialize the value as a string
     - This can be done using `#[specta(type = String)]` combined with a Serde `#[serde(with = "...")]` attribute.
     - Downside is that it can introduce annoying glue code, both on in Rust and in JS as you will need to turn it back into a `new BigInt(myString)` in JS.

 3. Use a Specta-based framework
     - Frameworks like [Tauri Specta](https://github.com/specta-rs/tauri-specta) and [TauRPC](https://github.com/MatsDK/TauRPC) take care of this for you.
     - They use special internals to preserve the values and make use of [`specta-tags`](http://docs.rs/specta-tags) for generating glue-code automatically.

 4. UNSAFE: Accept precision loss
     - Accept that large numbers may be deserialized differently and use `#[specta(type = f64)]` to bypass this warning on a per-field basis.
     - This can't be set globally as it is designed intentionally to introduce friction, as you are accepting the risk of data loss which is not okay.

</Callout>

### `Date` & `UInt8Array`

These are way easier as they don't require transport level changes so they should be working although I haven't had time to iron out the bugs they will inevitably have.

## Phase 6

From 27/03/2026 to 09/04/2026

The Specta repo is now down to 18 active GitHub issues which is amazing! Of the remaining issues none of them are marked as major breaking change which is really exciting!

I'm sorry for the relatively brief writeup this week!

### Released 2.0.0-rc.24

Did a release with all the improvements from the last sprint [Release notes](https://github.com/specta-rs/specta/releases/tag/v2.0.0-rc.24)!

This release was also finally coupled with a Tauri Specta release [release notes](https://github.com/specta-rs/tauri-specta/releases/tag/v2.0.0-rc.24)

The Tauri Specta release is launching the phase-specific types as an enabled by default feature.

### TauRPC

I closed the existing PR [#58](https://github.com/MatsDK/TauRPC/pull/58) for 2.0.0-rc.23 and opened up a new one for [#64](https://github.com/MatsDK/TauRPC/pull/64) 2.0.0-rc.24.

I also rebased this PR against the Flight Science fork [#12](https://github.com/fltsci/TauRPC/pull/12).

I would really appreciate any feedback while upgrading! I really want to see Flight Science get upgraded and also look for any holes in our Specta's testing process.

Once this PR is sorted we can introduce the typesafe error handling. I think it would be best to do one upgrade at a time to make upgrading less risky.

### Generics

We had a group of issues relating to generics which have been marked as `breaking-change` which I wanted to clear out.

#### Const generics

[#17](https://github.com/specta-rs/specta/issues/17)

Sadly Specta can't fully support const generics as we have no way to track their inference.

For generics we create a virtual type to replace it's implementation similar to:

```rust
pub struct MyType<A> {
    pub field: A
}

impl<A> Type for MyType<A> {
    fn definition(...) {
        // notice the shadowed name
        struct A;

        impl Type for A {
            fn definition(...) {
                GenericReference("A") // This is not a valid syntax but gives you the idea. We want `A` to become "A" in the `{ field: A }` instead of `{ field: String }` or whatever the concrete type is.
            }
        }

        // later we do something like:
        // <A as Type>::definition(...)
    }
}
```

but with const generics this doesn't work because the type will always be something like `usize`.

Another problem is our current implementation of `[T; N]` which is actually non-deterministic. If we always set the length in the `impl Type for [T; N]`, the implementation will "bake" whatever the first encountered value of `N` is into the global type definition which is wrong. For example:

```rust
pub struct A {
    a: Demo<1>,
    b: Demo<2>,
}
// becomes:
// export type A = { a: Demo, b: Demo }
// export type Demo = { [number] }; // This is invalid for the `b` field.

// and if we encounter the fields in the opposite order it changes:

pub struct B {
    // we flipped field definition
    b: Demo<2>,
    a: Demo<1>,
}
// becomes:
// export type A = { a: Demo, b: Demo }
// export type Demo = { [number, number] }; // This is invalid for the `a` field.
```

Your unlikely to notice this as Serde doesn't actually support `[T; N]` by default but with external crates or `with` implementations it is possible so it's still important that Specta is able to properly handle it.

To solve this I defined a set of rules:
 - If your in a named type with const generics we can't include length (Eg. always export as T[], instead of [T, T, T] (depending on length)).
 - If your in a named type without const generics we know that must be static (as associated const parameters aren't and can't be supported)
 - If your ouside the context of a named type length is safe.

With this I introduced [`CONTEXT_HAS_CONST_PARAMS`](https://github.com/specta-rs/specta/blob/3e93f7c07770885d79b153191a598f218a1e7b3f/specta/src/datatype/named.rs#L60) as a thread_local to track the scope and modify the type implementation according to the rules above.

I have introduced `specta_utils::FixedArray<N, T>` which can be used like `#[specta(type = specta_utils::FixedArray<4, usize>)]` to workaround Specta's conservative behavior in types with const generics.

### Bring back `DataType::Literal`

I started work on a solution to literal types as they would likely be required for const generics, however this work started before I figured out that we can't support const generics.

For now i've merged it into the repo but i'm leaving it commented out because I am not sure if we should stablise it.

Literals with floating-point types (`f32` and `f64`) are challenging as they can't be `Hash` or `Eq` which is a requirement on `DataType`'s in Specta.

They are also challenging because a number can be `NaN`, `Infinity` or `-Infinity` which can't be exported to Typescript as those aren't valid types. They would conventionally be under `number` but in this case we are attemping to export the literal type, not a general type. These would likely need to error in the Typescript exporter.

But all these questions and no requirement on supporting it right now means this can be shelved and potentially introduced post v2 if anyone is interested.

#### Default generics

[#16](https://github.com/specta-rs/specta/issues/16)

```rs
pub struct Demo<T = String> {
    t: T
}
```

now becomes

```ts
export type Demo<T = string> = T;
```

it also means references can be simplified from `Demo<string>` to `Demo` as the default generic is infered. This simplification is only possible if all generics are the default value as Typescript lacks something similar `_` in Rust which allows it to fallback to type inference.

### Cleanup accessors

[#431](https://github.com/specta-rs/specta/issues/431)

Instead of exposing:
 - `T::field(&self) -> &U`
 - `T::field(&mut self) -> &mut U`
 - `T::set_field(&mut self, v: U)`

I have just exposed the fields as `pub field: T`. This is cleaner and at the end of the day I am not sure the extra flexibility is going to be useful as the conctete type is already baked into most of the accessor methods anyway.

### `Exporter::map_fn`

[#470](https://github.com/specta-rs/specta/pull/470)

This is incomplete by the idea is to cleanup the code within TauRPC and Tauri Specta as right now it's complicated and importantly tricky to get correct.

Both frameworks run find+replace functions on the `DataType` layer to handling large number types, support Serde transforms and to support the Tauri channel type.

The idea here is to expose a hook from the Typescript exporter which allows us to define a central function to handle replacing any `DataType` which the Typescript exporter sees. This would prevent us from needing to manually remember to replace and apply transformations everywhere a `DataType` enters the system.

I am still not sure if this is going to work or not but it would be nice if something was possible as I am a little uncomfortable with how much logic is ending up in TauRPC.

### Tauri BigInt support

I also did more research into support bigint types in Tauri and wrote up [this RFC](https://hackmd.io/keQMrZ0cRbqmcUBaZL6MWQ). This is still not ready to present to the Tauri working group but once it's cleaned up I will.

I also became away that `NaN`, `Infinity` and `-Infinity` from floating point types are also lossily serialized which would also be nice to fix in this work. These types are actually different from bigint's through in that the issue for bigints is `JSON.parse`'s default lossy decoding, whereas these types are encoded as `null` in JSON which means the data is completly lost. I am not going to focus on these until regular bigints work but keeping them in mind is important in making a holistic solution.

## Phase 7 (future)

From 09/04/2026 to xx/04/2026

Core issues:
 - Finish TauRPC Typesafe error handling
 - `specta-tags` in TauRPC
 - Plan for dealing with `impl Type for *` without a semver nightmare, if it's even possible cause last time I looked it we have no real good options. Do up RFC with results.

 - Typescript and Serde exporter cleanup & better errors - [#475](https://github.com/specta-rs/specta/issues/475)
 - Overhaul examples - [#112](https://github.com/specta-rs/specta/issues/112)
 - Extend documentation for some more advanced information - [#299](https://github.com/specta-rs/specta/issues/299)
 - Fix and triage all remaining TODO's

Going to keep chipping away at:
- Nuanced types - [#203](https://github.com/specta-rs/specta/issues/203)

## Phase 8 (future)

From xx/xx/2026 to xx/xx/2026

Focus:
- Specta Zod - [#421](https://github.com/specta-rs/specta/pull/421)
- Specta OpenAPI??? - [#31](https://github.com/specta-rs/specta/issues/31)
- Figure out dropping `function` feature as this will be a breaking change. I started work on this but it would require changes to be upstreamed into Tauri.
    - Allows dropping `function` from core [#432](https://github.com/specta-rs/specta/pull/432)
    - `#[tauri::command(rename_all = "...")]` - [#213](https://github.com/specta-rs/specta/issues/213)

{/* Upstreaming changes in Flightscience forks (including [this](https://github.com/fltsci/plugins-workspace/tree/add-specta-to-updater)) */}

{/* Tauri Specta needs to be able to replace literals from `number` to `BigInt`. We need that as part of `Exporter::map`'s callback. */}
